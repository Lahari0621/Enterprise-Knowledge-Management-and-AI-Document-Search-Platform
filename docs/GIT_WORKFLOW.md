# Git Workflow
Use `main` for stable code, `develop` for integration, and `feature/*` branches for individual features. Use meaningful commits such as `Add authentication module`, `Add document management`, and `Add Docker configuration`.
